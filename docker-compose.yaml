services:
  app:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./database:/app/database/data
      - ./logs:/app/logs
    user: "1000:1000"
    restart: unless-stopped
    environment:
      - UMASK=002