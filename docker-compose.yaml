services:
  app:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - sqlite_data:/app/database/data
      - ./logs:/app/logs
    user: "app"
    restart: unless-stopped

volumes:
  sqlite_data:
    driver: local