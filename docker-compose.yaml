services:
  app:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - sqlite_data:/app/sqlite_data
    user: "app"

volumes:
  sqlite_data:
    driver: local